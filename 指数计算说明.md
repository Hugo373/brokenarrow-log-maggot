# Broken Arrow 玩家指数计算说明

> **版本归属**：本文档是参考算法 `ba_tool.maggot_index`（队内名次 + 余弦映射）的原始规格说明，保留用于对照。
> Web 界面线上使用的评分引擎为 `scoring.py`（队内连续相对成分 + 时间衰减 + 组排降权 + 贝叶斯收缩 + 置信区间），见 `ARCHITECTURE.md` 的「Player index pipeline」一节。

## 1. 指数是什么

本工具中的“玩家指数”指参考 `Zawinzala/Broken-Arrow-Maggot` 项目实现的 **Maggot Index / 蛆指数**。

它不是游戏官方评分，也不是反作弊结论。指数只用于根据近期公开对局数据，估计玩家在自己队伍中的相对表现。

指数范围：

```text
1.0 ～ 10.0
```

数值越低，表示近期队内排名越靠前；数值越高，表示近期队内排名越靠后。

## 2. 数据来源

工具通过公开统计接口获取数据：

```text
GET /api/analysis/player?stbid=<玩家ID>
GET /api/analysis/match?matchid=<对局ID>
```

其中：

- `stbid` 是 Broken Arrow 日志中的数字玩家 ID。
- `analysis/player` 提供玩家近期对局趋势和 ELO 变化。
- `analysis/match` 提供单局 `mvpRanking` 队伍排名数据。
- 原始 GameLogs 只用于识别当前对局和玩家名单，不会上传给第三方接口。

## 3. 为什么只使用有效对局

不是所有历史记录都适合用于指数计算。工具首先从玩家趋势中筛选有效候选对局。

一场对局必须满足：

1. 存在 `matchId`。
2. 存在 ELO 变化。
3. ELO 变化绝对值至少为 `0.01`。
4. 对局分析接口能够返回完整的 `mvpRanking`。
5. MVP 排名表至少包含 10 名玩家。
6. 目标玩家能够在该排名表中找到。

ELO 没有变化的记录通常可能是：

- 未完成对局。
- 无效对局。
- 掉线或取消的对局。
- 缺少结算信息的历史记录。
- API 不完整记录。

这些记录不会直接用于指数计算。

## 4. 对局选择范围

工具从玩家近期趋势中最多选择 30 场候选对局，并按照最近对局优先的顺序获取完整数据。

最终必须收集到 12 场完整有效对局，才会生成正式指数：

```text
候选对局：最多 30 场
正式计算：12 场
```

如果完整有效对局少于 12 场，工具不会用剩余数据猜测结果，而是显示：

```text
N/A (API has no index) / 暂无指数
```

## 5. 队内 MVP 名次

对于每一场有效对局，工具执行以下步骤：

### 5.1 找到目标玩家

在 `mvpRanking` 中根据 `playerId` 找到目标玩家。

### 5.2 找到目标玩家所在队伍

读取目标玩家的 `teamId`，只保留相同 `teamId` 的玩家。

因此，玩家不会和敌方玩家直接比较，而是在自己的队伍内部排名。

### 5.3 按 MVP 分数排序

队友按照 `score` 从高到低排序：

```text
score 越高 → 队内名次越靠前
```

例如：

| 玩家 | MVP 分数 | 队内名次 |
|---|---:|---:|
| A | 95.0 | 1 |
| B | 81.5 | 2 |
| C | 63.2 | 3 |
| D | 42.0 | 4 |
| E | 20.0 | 5 |

如果目标玩家是 C，则这一场的 `myRank` 为：

```text
3
```

注意：这里的名次是目标玩家在己方队伍中的名次，不是全场名次。

## 6. 平均名次

收集 12 场对局后，将每场的队内 MVP 名次求平均：

```text
averageRank = (rank1 + rank2 + ... + rank12) / 12
```

例如某玩家最近 12 场的队内名次是：

```text
1, 2, 3, 2, 4, 2, 1, 3, 2, 3, 2, 4
```

则：

```text
averageRank = 29 / 12
             = 2.4167
```

平均名次越接近 1，说明玩家越经常排在自己队伍前列。

## 7. 余弦曲线映射

平均名次不能直接作为最终指数，因为需要将：

```text
队内第 1 名 ～ 队内第 5 名
```

平滑映射到：

```text
指数 1.0 ～ 10.0
```

工具使用以下公式：

```text
normalizedRank = (averageRank - 1) / 4

curve = (1 - cos(normalizedRank × π)) / 2

maggotIndex = 1 + curve × 9
```

对应关系：

| 平均队内名次 | 归一化位置 | 指数约值 |
|---:|---:|---:|
| 1.0 | 0.00 | 1.0 |
| 2.0 | 0.25 | 2.3 |
| 3.0 | 0.50 | 5.5 |
| 4.0 | 0.75 | 8.7 |
| 5.0 | 1.00 | 10.0 |

余弦曲线的作用是让两端变化更平滑，避免简单线性换算造成的突兀跳变。

## 8. 指数等级

当前工具参考项目使用以下等级区间：

| 指数范围 | 中文标签 | 含义 |
|---:|---|---|
| `≤ 2.0` | `👑 神` | 近期经常位于队伍前列 |
| `> 2.0` 且 `≤ 4.0` | `🦁 团队支柱` | 通常能稳定贡献队伍表现 |
| `> 4.0` 且 `≤ 6.0` | `😐 平平淡淡` | 队内表现居中 |
| `> 6.0` 且 `≤ 8.0` | `🐛 有点蛆` | 近期排名偏后 |
| `> 8.0` | `💩 蛆！` | 近期经常位于队伍后列 |

这些标签是娱乐性描述，不是官方评价。

## 9. 对局开始时的工作流

实时模式检测到完整或基本完整的玩家列表后，会等待约 2 秒，让日志中的名单稳定下来：

```text
检测到 Start loading battle
    ↓
检测到 FID
    ↓
检测到 Player list
    ↓
等待名单稳定
    ↓
过滤机器人
    ↓
查询真人玩家公开数据
    ↓
获取近期有效对局
    ↓
计算每名玩家的指数
```

机器人通常使用负数 ID，例如：

```text
ID: -5, Name: Bot 0, Team: Bravo
```

机器人不会发起第三方 API 请求，并在报告中显示：

```text
BOT
```

## 10. API 异常和缓存行为

指数计算依赖第三方公开接口，因此可能遇到：

- 请求超时。
- HTTP 错误。
- 返回空数据。
- 返回格式变化。
- 统计数据缺失。
- 玩家未被收录。
- 有效对局不足 12 场。
- 服务端限流。

工具对此采用：

- 玩家档案缓存。
- 单局分析缓存。
- 指数缓存。
- 有限次数重试。
- 指数退避。
- 过期缓存回退。
- 无法恢复时显示明确错误。

工具不会在数据不足时生成伪造指数。

可能的结果包括：

```text
API unavailable / API不可用
N/A (API has no index) / 暂无指数
```

## 11. 当前指数的边界

当前实现的正式指数核心依据是：

```text
最近有效对局
+ ELO 变化
+ 队内 MVP 名次
+ 12 场平均值
+ 余弦曲线映射
```

玩家档案中显示的 ELO、胜率和 KD 是补充信息，不会直接混入上述指数公式。

击杀、损失、经济、占点等数据可以用于后续详细报告和行为标签，但当前 Maggot Index 的核心数值以队内 MVP 名次为准。

## 12. 隐私和安全

本工具：

- 只读取 Broken Arrow GameLogs。
- 不访问游戏内存。
- 不注入游戏进程。
- 不安装键盘或鼠标钩子。
- 不上传原始日志。
- 不修改游戏文件。
- 不将本地缓存内容写回游戏目录。

第三方 API 查询只发送玩家数字 ID或对局 ID，以及 API 所需的查询参数。
