# 1.2.0.2 热补丁兼容性检查

检查对象：Broken Arrow 1.2.0.2（2026-09-07 热补丁）。

## 官方变更

官方公告列出的变更：

- 扩展玩家登录失败日志。
- 修复玩家网关选择。
- 修复自定义场景中的 `SendSpawnUnit` 节点崩溃。
- 修复本地化问题。

参考：<https://store.steampowered.com/news/app/1604270/view/681886390551578163>

## 对本工具的影响

### 日志格式

“扩展玩家登录失败日志”会增加登录失败相关的日志行，但不会改变本工具依赖的核心对局标记：

```text
GetPersonaName
Start loading battle... map: ..., scenario: ...
FID:...
[NET_ROOM] GameRoom entered
Player list:
ID: ..., Name: ..., Team: ...
[NET_ROOM] GameRoom exited
GameController dispose called
```

当前解析器对未识别日志行采用忽略策略，因此新增登录失败详情不会破坏对局状态机。

### 网关选择

网关选择修复属于游戏网络连接行为。本工具只读取日志，不参与网关选择，也不修改网络配置，因此不受该修复的直接影响。

如果网关修复导致连接更稳定，可能间接增加完整对局结束记录；如果仍然发生断线，工具仍会保留已经解析到的当前对局和玩家名单。

### SendSpawnUnit

该修复属于自定义场景中的游戏逻辑，不影响日志读取、FID 解析或玩家列表解析。

### 本地化

地图名、场景名和玩家名可能出现新的语言或字符组合。当前实现：

- 使用 UTF-8 读取日志。
- 对非法字节使用替换字符，不因编码错误退出。
- 地图和场景按原始文本保留。
- 玩家名按原始文本保留。

因此本地化修复不会改变数据模型。

## 实机日志检查

当前可读取到的最新日志仍显示旧版本字符串：

```text
Version: 1.2.0.1
```

这是已经生成的旧日志文件的启动版本，不能证明当前游戏进程仍是旧版本。热补丁后的新启动日志需要在游戏重新启动后生成，才能确认新版本字符串。

但是，现有热补丁后的最新对局日志已经验证核心事件仍存在：

```text
Start loading battle... map: Ruda, scenario: - Ruda_network
FID:8647791
[NET_ROOM] GameRoom entered, fight id: 8647791
Player list:
[NET_ROOM] GameRoom exited
GameController dispose called
```

该日志被当前解析器成功解析为：

- FID：`8647791`
- 地图：`Ruda`
- 玩家：10 人
- 对局时长：约 2894 秒
- 对局正常结束事件：已识别

## WebUI 检查

当前 WebUI 能够正常读取最新日志状态：

```text
文件：Gamelog__2026_09_07__21_52.log
FID：8647791
玩家：10
统计条目：10
缓存失败：0
```

## 登录失败兼容性测试

已使用包含扩展登录失败详情的模拟日志行进行解析测试。结果：

- 登录失败详情被安全忽略。
- 后续战斗开始事件仍能创建对局。
- FID 仍能正确写入。
- 玩家列表仍能正确解析。

## 结论

当前证据表明，1.2.0.2 热补丁没有破坏本工具的核心工作流：

```text
日志监控 → 对局识别 → FID 识别 → 玩家列表 → 指数查询 → 局后报告 → WebUI 展示
```

兼容性结论：**核心流程暂时兼容，无需修改解析器。**

## 需要继续观察的部分

新版本首次启动后，建议检查：

1. 新日志中的 `Version:` 是否变为 `1.2.0.2`。
2. `Player list:` 是否仍紧跟标准玩家行。
3. FID 是否仍使用 `FID:<数字>` 格式。
4. `GameRoom entered/exited` 是否仍出现。
5. 局后是否仍出现 `GameController dispose called`。
6. 登录失败详情是否包含多行堆栈或新的前缀。
7. 新本地化文本是否包含特殊编码或换行。

只要以上核心标记不变，现有工作流不需要调整。
