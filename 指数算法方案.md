# 玩家综合指数算法方案

本文提出四种侧重点不同、但都保持综合性的玩家指数方案。它们都使用同一套标准化输入，便于在 WebUI 中并列展示或进行加权融合。

## 一、统一输入数据

优先使用最近 12 场有效排位对局；候选上限 30 场。有效对局需要有对局 ID、有效玩家记录和可比较的结算数据。

每场对局可使用以下指标：

- `mvpRank`：玩家在己方队伍中的 MVP 名次，1 为最佳。
- `mvpScore`：MVP 综合分。
- `killValue`：击杀或摧毁价值。
- `lossValue`：损失价值，越低越好。
- `kdRatio`：击杀价值与损失价值比。
- `objectiveValue`：占点或目标贡献。
- `damageDealt`：造成伤害。
- `damageTaken`：承受伤害。
- `supplyValue`：补给贡献。
- `eloDelta`：本局 ELO 变化。
- `won`：胜负结果。
- `matchCompleteness`：该局数据完整度。

不能直接比较原始数值。应先按“同局、同队”进行百分位或相对排名标准化：

```text
percentile(x) = 玩家在己方队伍中的相对百分位
```

对于损失、承伤等“越低越好”的指标，使用：

```text
reversePercentile(x) = 1 - percentile(x)
```

这样所有指标都统一为：

```text
数值越高 = 表现越好
```

## 二、方案 A：队内 MVP 名次指数

### 侧重点

突出玩家在队伍中的综合结果位置。最稳定、最容易解释，适合做默认主指数。

### 计算

每局先取得玩家队内 MVP 名次：

```text
rankScore = 1 - (mvpRank - 1) / max(队伍人数 - 1, 1)
```

1 名为 1.0，队内末位为 0.0。

12 场使用时间衰减平均：

```text
weight(i) = exp(-0.08 × i)

base = Σ(rankScore(i) × weight(i)) / Σ(weight(i))
```

最近对局权重更高。

映射为 1–10 指数：

```text
index = 10 - 9 × base
```

### 优点

- 与原 Maggot Index 的核心思想最接近。
- 只需 MVP 表即可计算。
- 结果容易向用户解释。
- 对不同队伍人数有适配能力。

### 缺点

- 依赖第三方 MVP 排名。
- 可能忽略不同打法的实际贡献。
- 不能很好区分两个 MVP 名次相同但行为差异很大的玩家。

## 三、方案 B：战斗贡献综合指数

### 侧重点

突出玩家在战斗中的实际投入和产出，减少单独依赖 MVP 排名。

### 单局分数

推荐权重：

```text
击杀价值       25%
KD/交换效率    20%
MVP 分数       20%
目标/占点       15%
伤害贡献       10%
补给贡献        5%
胜负结果        5%
```

公式：

```text
combatScore =
    0.25 × killScore
  + 0.20 × kdScore
  + 0.20 × mvpScore
  + 0.15 × objectiveScore
  + 0.10 × damageScore
  + 0.05 × supplyScore
  + 0.05 × winScore
```

其中：

```text
winScore = 1.0  胜利
           0.5  平局或未知
           0.0  失败
```

最终指数：

```text
index = 10 - 9 × weightedMean(combatScore)
```

### 优点

- 能体现击杀、目标、伤害、补给等多种打法。
- 不会把 MVP 名次作为唯一依据。
- 更接近“这一局到底做了什么”。

### 缺点

- 权重会影响结果，需要公开说明。
- 不同阵营、地图和打法可能存在统计偏差。
- 数据字段缺失时需要可靠降级，不能直接把缺失当成 0。

### 缺失字段处理

如果某项缺失，应重新归一化可用权重：

```text
实际分数 = 可用项加权和 / 可用权重总和
```

不能因为 API 没返回补给数据，就把玩家补给贡献判为零。

## 四、方案 C：结果与效率指数

### 侧重点

突出“用多少资源换来多少成果”，适合评价经济交换、单位保存和高效打法。

### 单局分数

```text
efficiencyScore =
    0.30 × reverseLossScore
  + 0.25 × kdScore
  + 0.20 × killValueScore
  + 0.10 × damageEfficiencyScore
  + 0.10 × objectiveEfficiencyScore
  + 0.05 × mvpScore
```

伤害效率可定义为：

```text
damageEfficiency = damageDealt / max(damageTaken, 基准值)
```

资源交换效率可定义为：

```text
exchangeEfficiency = killValue / max(lossValue, 基准值)
```

指数：

```text
index = 10 - 9 × weightedMean(efficiencyScore)
```

### 优点

- 能识别低损失、高交换效率的玩家。
- 不会只奖励无脑堆资源或单纯占点。
- 对装甲、炮兵、支援等非传统击杀打法更公平。

### 缺点

- 对补给、侦察、牵制等难量化行为覆盖不足。
- 极端小损失可能导致 KD/效率虚高。
- 必须使用截断、平滑和最小样本保护。

### 必要保护

```text
kdRatio = min(kdRatio, P99)
```

或使用对数：

```text
kdScore = log(1 + kdRatio) / log(1 + P99)
```

避免单个异常值完全主导指数。

## 五、方案 D：团队价值指数

### 侧重点

突出玩家对胜利和队伍目标的贡献，不只评价个人战斗数据。

### 单局分数


```text
teamScore =
    0.25 × winScore
  + 0.20 × objectiveScore
  + 0.20 × mvpScore
  + 0.15 × killScore
  + 0.10 × supplyScore
  + 0.05 × damageScore
  + 0.05 × survivalScore
```

其中 `survivalScore` 可以由损失价值反向标准化得到。

可以增加“队伍超额贡献”：

```text
overExpected = 玩家得分 - 己方队伍平均得分
```

最终：

```text
teamValue = 0.75 × teamScore + 0.25 × overExpectedScore
index = 10 - 9 × weightedMean(teamValue)
```

### 优点

- 奖励胜利、占点、补给和团队配合。
- 更适合评价支援和目标型玩家。
- 能体现“没有最高击杀，但帮助队伍获胜”的表现。

### 缺点

- 胜负会放大团队因素，难完全归因到个人。
- 单人带队和队友拖累可能混在一起。
- 自定义局或数据不完整对结果影响较大。

## 六、方案 E：稳健贝叶斯综合指数

### 侧重点

突出长期稳定性，减少样本少、异常局和数据缺失造成的误判。

### 核心思想

不直接使用简单平均，而是使用先验均值进行收缩：

```text
adjustedScore =
    (n × playerMean + priorN × populationMean)
    / (n + priorN)
```

建议：

```text
priorN = 4
```

玩家只有 1–2 场有效对局时，结果会向总体均值靠拢；达到 12 场后，玩家自身数据占主导。

综合维度：

```text
stableScore =
    0.30 × mvpScore
  + 0.20 × combatScore
  + 0.20 × efficiencyScore
  + 0.15 × objectiveScore
  + 0.15 × winScore
```

再进行贝叶斯收缩：

```text
finalScore = shrink(stableScore, populationMean, n, priorN)
index = 10 - 9 × finalScore
```

### 优点

- 对小样本最稳健。
- 能处理有效数据不足但仍希望显示“暂定指数”的场景。
- 可附带可信度区间。

### 缺点

- 用户不容易直观理解。
- 需要维护总体基准和样本统计。
- 不能与“严格 12 场才出正式指数”混为一谈。

## 七、推荐的最终产品方案

不建议只选一个指数。推荐 WebUI 同时提供“主指数 + 分项雷达 + 可信度”。

### 主指数

采用方案 A 和方案 B 的融合：

```text
综合指数 =
    0.40 × 队内 MVP 名次指数
  + 0.35 × 战斗贡献指数
  + 0.15 × 团队价值指数
  + 0.10 × 效率指数
```

仍然映射到 `1.0–10.0`。

### 分项展示

同时展示四个 0–100 分的分项：

```text
战斗：击杀、伤害、KD
目标：占点、任务、胜负
效率：损失、资源交换、单位保存
团队：补给、队伍超额贡献、MVP
```

### 推荐 UI 表现

#### 1. 主指数卡片

```text
综合指数 4.2
团队支柱
```

配合颜色渐变，但不能只依赖颜色：

```text
1.0–2.0  神
2.0–4.0  团队支柱
4.0–6.0  中等
6.0–8.0  偏弱
8.0–10.0 蛆
```

#### 2. 横向分数条

```text
战斗 78/100  ███████████████░░░░░
目标 61/100  ████████████░░░░░░░░
效率 84/100  █████████████████░░░
团队 70/100  ██████████████░░░░░░
```

#### 3. 队内位置

```text
本局队内：第 2/5
最近 12 局平均：第 2.4/5
趋势：上升
```

#### 4. 指数构成说明

```text
MVP 名次       40%  3.1
战斗贡献       35%  4.8
团队价值       15%  3.9
效率           10%  2.7
```

#### 5. 可信度标记

```text
数据覆盖：12/12
字段完整度：91%
可信度：高
更新时间：2026-09-08 12:30
```

## 八、综合性保证

无论采用哪种方案，都应满足以下约束：

1. 至少包含个人战斗、目标贡献、效率和团队价值中的三个维度。
2. 不让单一指标占比超过 40%。
3. 缺失字段时重新归一化权重，不把缺失误判为零贡献。
4. 对异常值进行截断或对数平滑。
5. 同时显示样本数量和数据完整度。
6. 区分正式指数、暂定指数和无指数。
7. 给出指数构成，保证结果可解释。
8. 保留最近对局趋势，不能只显示静态总分。
9. 机器人、观战局、未结算局不进入个人排位指数。
10. 自定义局与排位局分开统计，避免混合造成偏差。

## 九、最终推荐

第一版推荐：

```text
主指数：方案 A + B + C + D 的加权融合
稳定机制：方案 E 的小样本收缩
展示方式：主指数 + 四项分数条 + 队内排名 + 趋势 + 可信度
```

默认公式：

```text
综合指数 =
    0.40 × MVP 名次
  + 0.35 × 战斗贡献
  + 0.15 × 团队价值
  + 0.10 × 效率
```

如果用户需要最接近原版 Maggot Index 的结果，使用方案 A；如果更重视实际战斗表现，使用方案 B；如果更重视资源交换和生存，使用方案 C；如果更重视胜利与团队协作，使用方案 D；如果更重视长期稳定和小样本可靠性，加入方案 E。
## 十、当前实现状态

当前代码已加入独立的 `scoring.py` 和 `analysis_engine.py`：

- 使用连续 MVP 分数，而不是只使用队内序数名次。
- 使用击杀价值、损失价值、伤害、目标、补给和 MVP 等多维数据。
- 缺失字段会触发可用权重重新归一化，不会自动判为零分。
- 使用中位数/IQR 和 Logistic 函数抑制异常值。
- 使用最近对局时间衰减：`0.92^i`。
- 使用小样本收缩，避免少量对局产生过度确定的结果。
- 输出样本数、数据覆盖率和 90% 不确定区间。
- 玩家未出现在 MVP 表时不会被当作队内第 5 名。
- 机器人不参与真人表现指数。

当前 WebUI 提供四个分页：

```text
工作情况
局前速览
局后复盘
诊断
```

页面会根据日志事件自动切换到局前或局后页面。局前页面展示玩家综合表现、样本和置信区间；局后页面展示本局队伍综合得分、玩家实际贡献字段和人类可读报告；诊断页面展示解析器健康、API 熔断和缓存状态。
